<?xml version="1.0" encoding="utf-8"?>
<!-- v1.15 BrandMark — bitmap foreground sourced from the artwork PNG.
     Wrapped in an inset so it sits inside the adaptive icon safe zone
     (icon canvas is 108dp / 432px, safe zone ~66dp / 264px). The
     22% inset on each side keeps the gem mosaic visible after the
     launcher applies its mask + scaling envelope. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_brand_logo"
    android:inset="22%" />
